# vim:ft=yaml.ansible
---
- name: Darwin | Install brew cask packages
  community.general.homebrew_cask:
    name: "{{ item }}"
    state: present
    allow_external_apps: true
  loop:
    - dbeaver-community
    - postman
    - virtualbox
